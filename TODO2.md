# TODO2

- [x] Move `<Cards />` into `src/components/Layout.jsx` so it appears on all pages.
- [x] Remove `<Cards />` import/render from `src/pages/Dashboard.jsx` to avoid duplication.
- [x] `npm run build` to confirm no syntax errors.



